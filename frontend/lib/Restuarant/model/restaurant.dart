class Restaurant {
  Restaurant({
    required this.img,
    required this.name,
    required this.id,
    required this.description,
    // required this.category});
  });
  String id, img, name, description;
}
